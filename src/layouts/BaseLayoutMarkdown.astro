---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  author={frontmatter.author}
>
  <article class="calistenia-article max-w-none">
    <slot />
  </article>
</BaseLayout>